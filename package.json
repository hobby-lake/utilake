{
  "name": "bb-plugin-template-esbuild",
  "version": "1.0.0",
  "description": "A template for building Blockbench plugins using Typescript and esbuild",
  "main": "src/plugin.ts",
  "type": "module",
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --outfile=dist/utilake.js",
    "dev": "esbuild src/plugin.ts --bundle --outfile=dist/utilake.js --watch"
  },
  "author": "JannisX11",
  "license": "MIT",
  "dependencies": {
    "blockbench-types": "^5.0.0-beta.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
